unbind C-b
set -g prefix C-a
set -g default-terminal "xterm-256color"

bind-key a last-window

# start window index at 1
set -g base-index 1

# start pane index at 1
setw -g pane-base-index 1

# pane movement
bind-key J command-prompt -p "join pane from:"  "join-pane -s '%%'"
bind-key S command-prompt -p "send pane to:"  "join-pane -t '%%'"

# Reload source file
bind-key r source-file ~/.tmux.conf \; display-message "Configuration reloaded"

# Fix clipboard support
#set-option -g default-command "reattach-to-user-namespace -l bash"

# Open man page for given command
bind C-m command-prompt -p "Open man page for:" "new-window 'exec man %%'"

# Pane movement
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Quick pane cycling
unbind ^A
bind ^A select-pane -t :.+

# Pane resizing
bind -r Left  resize-pane -L 2
bind -r Down  resize-pane -D 2
bind -r Up    resize-pane -U 2
bind -r Right resize-pane -R 2

# Saner splitting.
bind v split-window -h -p 25
bind s split-window -v -p 25

# Activity
setw -g monitor-activity on
set -g visual-activity off
set-window-option -g window-status-activity-attr bold,blink,underscore

# Autorename sanely.
#setw -g automatic-rename off

# Better name management
bind c new-window
bind , command-prompt "rename-window '%%'"

# Bind for `choose-session`
bind C choose-session

# Copy mode
setw -g mode-keys vi
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-pipe "parcellite"
bind-key -t vi-copy 'r' rectangle-toggle

# Copy top tmux buffer into system cliboard
#bind y run-shell "reattach-to-user-namespace -l bash -c 'tmux show-buffer | pbcopy'" \; display-message "Copied tmux buffer to system clipboard"

###########################
# Status Bar
###########################

# enable UTF-8 support in status bar
#set -g status-utf8 on

# set refresh interval for status bar
set -g status-interval 30

# center the status bar
set -g status-justify left

# show session, window, pane in left status bar
set -g status-left-length 40
set -g status-left ' #[fg=colour196]#(if [ "$SSH_CONNECTION" ]; then echo "☎ | "; fi)#[fg=colour106]#S#[fg=colour39] #I:#P #[default]'

# show hostname, date, time, and battery in right status bar
set-option -g status-right '#[fg=colour106]#H #[fg=colour238]| #[fg=colour249] %m/%d/%y #[fg=colour238]| #[fg=colour45]%I:%M #[fg=colour203]#(battery discharging)#[default]#(battery charging)'

############################
## Colors
############################

## --- colors (solarized dark)

## default statusbar colors
#set -g status-bg black
#set -g status-fg yellow
#set -g status-attr default

## default window title colors
#setw -g window-status-fg brightblue
#setw -g window-status-bg default

## active window title colors
#setw -g window-status-current-fg yellow
#setw -g window-status-current-bg default
#setw -g window-status-current-attr dim

## pane border
#set -g pane-border-fg black
#set -g pane-border-bg default
#set -g pane-active-border-fg yellow
#set -g pane-active-border-bg default

## command line/message text
#set -g message-bg black
#set -g message-fg yellow

## pane number display
#set -g display-panes-active-colour yellow
#set -g display-panes-colour brightblue

## clock
setw -g clock-mode-colour yellow

################################################################################
# Powerline Cyan Block - Tmux Theme
# Created by Jim Myhrberg <contact@jimeh.me>.
#
# Inspired by vim-powerline: https://github.com/Lokaltog/powerline
#
# Requires terminal to be using a powerline compatible font, find one here:
# https://github.com/Lokaltog/powerline-fonts
################################################################################

# Status update interval
set -g status-interval 1

# Basic status bar colors
set -g status-fg colour240
set -g status-bg colour236

# Left side of status bar
set -g status-left-bg colour236
set -g status-left-fg colour243

# Right side of status bar
set -g status-right-bg colour236
set -g status-right-fg colour243

# Window status
set -g window-status-format " #I:#W#F "
set -g window-status-activity-format " #I:#W#F "
set -g window-status-current-format " #I:#W#F "

# Current window status
set -g window-status-current-bg colour74
set -g window-status-current-fg colour232

# Window with activity status
set -g window-status-activity-bg colour236 # fg and bg are flipped here due to
set -g window-status-activity-fg colour247 # a bug in tmux

# Window separator
set -g window-status-separator ""

# Window status alignment
set -g status-justify centre

# Pane border
set -g pane-border-bg default
set -g pane-border-fg colour238

# Active pane border
set -g pane-active-border-bg default
set -g pane-active-border-fg colour118

# Pane number indicator
set -g display-panes-colour colour233
set -g display-panes-active-colour colour245

# Clock mode
set -g clock-mode-colour colour39
set -g clock-mode-style 24

# Message
set -g message-bg colour39
set -g message-fg black

# Command message
set -g message-command-bg colour233
set -g message-command-fg black

# Mode
set -g mode-bg colour39
set -g mode-fg colour232
